<h5>Filter By Skill Level</h5>
<%= form_for(@users_sport, url: location_sport_skill_path(@location, @sport, @date)) do |f| %>
  <%= f.select :skill_level, ['Beginner', 'Intermediate', 'Advanced'], {}, {:class => 'browser-default'} %>
  <%= f.submit "Filter" %>
<% end %>
