<%= form_for(@users_sport, url: location_sport_skill_path(@location, @sport)) do |f| %>
  <%= f.select :skill_level, ['Beginner', 'Intermediate', 'Pro'], {}, {:class => 'browser-default'} %>
  <%= f.submit "Change Skill" %>
<% end %>