<div class="row">
  <div class="col s12 m6 offset-m3">
    <div class="card-panel blue-grey darken-2">
      <%= link_to "<-", location_sport_path(@location, @sport, date: @date - 1.day) %> <%= @date_display %> <%= link_to "->", location_sport_path(@location, @sport, date: @date + 1.day) %>
<br>
<% @games.each do |game| %>
  <%= render partial: "games", locals: {game: game} %>
<% end %>

<%= render partial:"new_game_link" %>

<%= form_for(@sport, url: location_sport_navigate_path(@location, @sport)) do |f| %>
  <%= render partial: "navigate_form", locals: {f: f} %>
<% end %>
    </div>
  </div>
</div>
